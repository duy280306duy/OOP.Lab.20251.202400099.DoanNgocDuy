	If all media items share the same natural ordering (for example, by title then cost), the Comparable interface should be implemented in the superclass Media. If different media types require different ordering rules, each subclass (like DigitalVideoDisc, Book, CompactDisc) should implement Comparable separately.
	The compareTo() method should compare fields in the order of priority. This ensures that items are first ordered by title, and if titles are equal, by cost.
	No. The Comparable interface only defines a single natural ordering for a class. If you need multiple ordering rules, you must use the Comparator interface instead, which allows you to define as many different sorting strategies as needed.
	You can implement Comparable specifically in the DigitalVideoDisc class with its own compareTo() method.This allows DVDs to have a custom ordering independent of other media types. If you want to sort a mixed collection of different media types, you would still need a Comparator to define cross-type comparisons.